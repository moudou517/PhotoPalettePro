<?xml version="1.0" encoding="utf-8"?>
<!--
	悬浮玻璃栏（顶栏 / 底栏）。

	和卡片同一套做法：一个三段渐变表达「上沿高光 + 一整片均匀的面」，
	外加一层发丝描边。不要用两层半透明白去叠——那会在真机上叠出色差。
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
	<item>
		<shape android:shape="rectangle">
			<gradient
					android:angle="270"
					android:startColor="@color/glass_bar_top"
					android:centerColor="@color/glass_bar_mid"
					android:centerY="0.12"
					android:endColor="@color/glass_bar_bottom" />
			<corners android:radius="24dp" />
		</shape>
	</item>

	<item>
		<shape android:shape="rectangle">
			<stroke android:width="1dp" android:color="@color/glass_border" />
			<corners android:radius="24dp" />
		</shape>
	</item>
</layer-list>
